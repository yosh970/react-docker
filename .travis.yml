language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t yosh970/react-docker -f Dockerfile.dev .

script:
  - docker run yosh970/react-docker npm run test -- --coverage